# Variables opcionales para docker compose (cópialo a .env si quieres cambiarlas).
# Todas tienen valor por defecto, así que el entorno arranca sin este archivo.

JWT_SECRET=dev-secret-cambiar-en-produccion
DEMO_ADMIN_PASSWORD=Admin123!
DEMO_USER_PASSWORD=User123!
